package com.google.gooddoctor.view;

import android.content.Context;
import android.content.res.ColorStateList;
import android.support.annotation.Nullable;

public interface CardViewImpl {
    float a(CardViewDelegate cardViewDelegate);

    void a();

    void a(CardViewDelegate cardViewDelegate, float f);

    void a(CardViewDelegate cardViewDelegate, Context context, ColorStateList colorStateList, float f, float f2, float f3, ColorStateList colorStateList2, ColorStateList colorStateList3);

    void a(CardViewDelegate cardViewDelegate, @Nullable ColorStateList colorStateList);

    float b(CardViewDelegate cardViewDelegate);

    void b(CardViewDelegate cardViewDelegate, float f);

    float c(CardViewDelegate cardViewDelegate);

    void c(CardViewDelegate cardViewDelegate, float f);

    float d(CardViewDelegate cardViewDelegate);

    float e(CardViewDelegate cardViewDelegate);

    void g(CardViewDelegate cardViewDelegate);

    void h(CardViewDelegate cardViewDelegate);

    ColorStateList i(CardViewDelegate cardViewDelegate);
}
